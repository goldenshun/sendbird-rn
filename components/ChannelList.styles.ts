import { css } from "@emotion/native";

const styles = css`
  flex: 1;
`;

export const addButtonStyles = css`
  position: absolute;
  margin: 16px;
  right: 0;
  bottom: 0;
`;

export default styles;
